const names = ['Alysson', 'Alberto', 'Seu Madruga', 'Chaves'];

for(let i = 0; i < names.length; i++){
    let letra_a = names[i].substring(0,1);
    
    if(letra_a === 'A'){
        console.log(names[i]);
    }
}

const my_array = [1,2,3,4,5,'a', 'b', 'c'];

console.log(my_array);
console.log(my_array[my_array.length-1]);





